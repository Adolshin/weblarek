# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом
- src/components/Models/ — папка с классами слоя модели данных
- src/components/Communication/ — папка с классом коммуникационного слоя
- src/components/View/ — папка с классами слоя представления

Важные файлы:

- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```

## Сборка

```
npm run build
```

или

```
yarn build
```

# Интернет-магазин «Web-Larёk»

«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component

Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
- `constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
- `container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
- `render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
- `setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`

#### Класс Api

Содержит в себе базовую логику отправки запросов.

Конструктор:  
- `constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
- `baseUrl: string` - базовый адрес сервера  
- `options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
- `get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
- `post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
- `handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter

Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
- `_events: Map<string | RegExp, Set<Function>>)` - хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
- `on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
- `emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
- `trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные

Интерфейс IProduct описываеет тип объекта данных товара, включает в себя id товара, название, изображение, описание, категорию и цену

```
interface IProduct {
  id: string;
  title: string;
  image: string;
  description: string;
  category: string;
  price: number | null;
}
```

Интерфейс IBuyer описываеет тип объекта данных покупателя, включает в себя тип оплаты, email, телефон и адрес доставки

```
interface IBuyer {
  payment: 'cash'|'online'|'';
  email: string;
  phone: string;
  address: string;
}
```

Интерфейс IOrder описывает тип объекта заказа отправляемого на сервер в post запросе, расширяет интерфейс IBuyer, добавляя два поля, - стоимость заказа и массив с id заказанных товаров

```
interface IOrder extends IBuyer {
  total: number;
  items: string[];
}
```

Интерфейс IErrors описывает тип объекта ошибок возвращаемого методом валидации, наследует интерфейс IBuyer меняя тип одного из полей

```
interface IErrors extends Omit<IBuyer, 'payment'>  {
  payment: string;
}
```

Интерфейс IProductListResponse, описывает тип объекта возвращаемого в промисе сервером, включает в себя массив объектов типа IProduct и кол-во таких объектов в массиве

```
interface IProductListResponse {
  total: number;
  items: IProduct[];
}
```

Интерфейс IOrderResponse описывает тип объекта ответа сервера на post запрос (подтверждение заказа), включает в себя id заказа и стоимость заказа

```
interface IOrderResponse {
  id: string;
  total: number;
}
```




### Слой Модели данных

#### Класс CatalogModel

Класс отвечает за хранение данных о товарах доступных для просмотра и логику работу с этими данными

Конструктор класса:
- `constructor(events: IEvents)` - принимает экземпляр класса EventEmitter

Полях класса:
- `events: IEvents` - экземпляр класса EventEmitter
- `productList: IProduct[]` - массив объектов товаров.
- `product?: IProduct` - выбранный товар(карточка).
  Методы класса:
- `setProductList(data: IProduct[]): void` - принимает массив обьектов товаров и устаналивает соотвектвующее поле класса.
- `getProductList(): IProduct[]` - возвращает массив обьектов товаров из поля класса.
- `getProductById(id: string): IProduct|undefined` - принимает id товара и возвращает объект товара из поля класса.
- `setProduct(id: string): void` - принимает id товара и устанавливает поле класса выбранный товар.
- `getProduct(): IProduct | undefined` - возвращает выбранный товар из соответвующего поля.

#### Класс BasketModel

Класс отвечает за хранение данных о товарах, добавленных в корзину и логику работы с этими данными.

Конструктор класса:
- `constructor(events: IEvents)` - принимает экземпляр класса EventEmitter

Поля класса:
- `events: IEvents` - экземпляр класса EventEmitter
- `productList: IProduct[]` - массив объектов товаров, добавленных в корзину.

Методы класса:
- `getProductList(): IProduct[]` - возвращает массив товаров из поля класса.
- `addProduct(product: IProduct): void` - принимает обьект товара и добавляет товар в поле класса.
- `deleteProduct(id: string): void` - принимает id товара и удаляет товар из поля класса.
- `clearBasket(): void` - очищает поле класса.
- `getTotalPrice(): number` - возвращает общую стоимость товаров добавленных в поле класса.
- `getProductQuantity(): number` - возвращает кол-во товаров в поле класса.
- `checkProduct(id: string): boolean` - принимает id товара и возвращает информацию о наличии товара в поле класса.

#### Класс BuyerModel

Класс отвечает за хранение данных покупателя логику работы с этими данными.

Конструктор класса:
- `constructor(events: IEvents)` - принимает экземпляр класса EventEmitter

Поля класса:
- `events: IEvents` - экземпляр класса EventEmitter
- `data: IBuyer` - объект данных покупателя(вид оплаты, адрес, телефон, email).

Методы класса:
- `setData(userData: Partial<IBuyer>): void` - принимает обьект данных покупателя и сохраняет его в поле класса.
- `getData(): IBuyer` - возвращает объект данных покупателя из поля класса.
- `clearData(): void` - очищает поле класса.
- `validateData(): Partial<IErrors>` - валидирует данные в поле класса по условию "поле должно быть заполнено" и возвращает обьект с ошибками валидации.

### Слой коммуникации

#### Класс WeblarekApi

Отвечает за взаимодействие с сервером проекта.

Конструктор класса:
- `constructor(baseApi: IApi, cdn: string)` - принимает экземпляр класса Api и адрес сервера отдающего изображения товаров

Поля класса:
- `baseApi: IApi` - поле для хванения экземпляра базового класса Api принимаего в конструкторе.
- `cdn: string` - поле для хванения адреса сервера изображений товаров

Методы класса:
- `getProductList():Promise<IProduct[]>` - метод для осуществления get запроса на эндпонт /product/, возвращает промис с массивом товаров.
- `postOrder(data:IOrder):Promise<IOrderResponse>` - метод для осуществления post запроса на эндпонт /order/. (возможна доработка для других типов запросов предусмотренных в базовом классе которые на данный момент не требуются), возвращает промис с обьектом подвежденного заказа

### Слой представления

#### Класс HeaderView
Является производным от базового класса Component. Отвечает за рендеринг счетчика количества товаров, добавленных в корзину и обработку событий клика по кнопке корзины товаров

Конструктор:  
- `constructor(events: IEvents, container: HTMLElement)` - принимает экземпляр класса EventEmitter и ссылку на DOM элемент, за отображение которого он отвечает, для передачи в конструктор родительского класса

Поля класса:
- `events: IEvents` - экземпляр класса EventEmitter
- `buttonElement: HTMLButtonElement` - DOM элемент кнопки корзины товаров
- `counterElement: HTMLElement` - DOM элемент счетчика товаров, добавленных в корзину

Методы класса:
- `set counter(value: number)` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение счетчика товаров, добавленных в корзину

Вспомогательный интерфейс:
Интерфейс IHeader описывает тип данных, который принимает метод render родительского класса, передается дженериком

```
interface IHeader {
  counter: number;
}
```


#### Класс CatalogView
Является производным от базового класса Component. Отвечает за рендеринг каталога товаров на странице

Конструктор:  
- `constructor(container: HTMLElement)` - принимает ссылку на DOM элемент, за отображение которого он отвечает, для передачи в конструктор родительского класса

Поля класса:
Класс не имеет собвенных полей, наследуя поля родительского класса

Методы класса:
- `set content(items: HTMLElement[])` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса,отвечает за отображение содержимого каталога продуктов

Вспомогательный интерфейс:
Интерфейс ICatalog описывает тип данных, который принимает метод render родительского класса, передается дженериком

```
interface ICatalog {
  content: HTMLElement[];
}
```

#### Класс BasketView
Является производным от базового класса Component. Отвечает за рендеринг корзины товаров

Конструктор:  
- `constructor(events: IEvents, container: HTMLElement)` - принимает экземпляр класса EventEmitter и ссылку на DOM элемент, за отображение которого он отвечает, для передачи в конструктор родительского класса

Поля класса:
- `events: IEvents` - экземпляр класса EventEmitter
- `buttonElement: HTMLButtonElement` - DOM элемент кнопки оформления товаров
- `contentElement: HTMLElement` - DOM элемент для отображения списка товаров
- `priceElement: HTMLElement` - DOM элемент для отображения общей стомости товаров

Методы класса:
- `set content(items: HTMLElement[])` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение списка товаров
- `set price(value: number)`- - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение общей стомости товаров

Вспомогательный интерфейс:
Интерфейс IBasket описывает тип данных, который принимает метод render родительского класса, передается дженериком

```
interface IBasket {
  content: HTMLElement[];
  price: number;
}
```
#### Класс ModalView
Является производным от базового класса Component. Отвечает за рендеринг модального окна без содержимого

Конструктор:  
- `constructor(container: HTMLElement, pageContainer: HTMLElement)` - принимает ссылку на DOM элемент, за отображение которого он отвечает, для передачи в конструктор родительского класса и ссылку на элемент страницы, овечающий за блокировку скролла модального окна

Поля класса:
- `contentElement: HTMLElement` - DOM элемент для отображения содержимого модального окна
- `buttonElement: HTMLButtonElement` - DOM элемент кнопки закрытия модального окна
- `pageContainer: HTMLElement` - DOM элемент, овечающий за блокировку скролла модального окна

Методы класса:
- `set content(items: HTMLElement[])` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение содержимого модального окна
- `handleClick = (evt: MouseEvent)` - метод обработки события для закрытия модального окна с помощью клика мышью
- `handleEscape = (evt: KeyboardEvent)` - метод обработки события для закрытия модального окна с помощью клавиатуры
- `closeModal()` - метод для закрытия модального окна
- `openModal()` - - метод для открытия модального окна

Вспомогательный интерфейс:
Интерфейс IModal описывает тип данных, который принимает метод render родительского класса, передается дженериком

```
interface IModal {
  content: HTMLElement;
}
```

#### Класс CardView
Является производным от базового класса Component. Общий абстрактный класс, который наследуют классы отвечающие за рендеринг карточек товара 

Конструктор:  
- `constructor(container: HTMLElement)` - принимает ссылку на DOM элемент, за отображение которого он отвечает, для передачи в конструктор родительского класса

Поля класса:
- `titleElement: HTMLElement` - DOM элемент для отображения названия товара
- `priceElement: HTMLElement` - DOM элемент для отображения цены товара


Методы класса:
- `set title(value: string` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение названия товара
- `set price(value: number)` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение цены товара


Вспомогательные типы:
Тип TCard описывает тип данных, который принимает метод render родительского класса, передается дженериком

```
type TCard = Pick<IProduct, "title" | "price">;
```

#### Класс CardFullView
Является производным от класса CardView. Общий абстрактный класс, который наследуют классы отвечающие за рендеринг карточек товара для каталога и для превью

Конструктор:  
- `constructor(container: HTMLElement)` - принимает ссылку на DOM элемент, за отображение которого он отвечает, для передачи в конструктор родительского класса

Поля класса:
- `categoryElement: HTMLElement` - DOM элемент для отображения категории товара
- `imageElement: HTMLImageElement` - DOM элемент для отображения изображения товара


Методы класса:
- `set category(value: string)` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение категории товара
- `set image(value: string)` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение изображения товара


Вспомогательные типы:
Тип TCardFull описывает тип данных, который принимает метод render родительского класса, передается дженериком

```
type TCardFull = Pick<IProduct, "image" | "category">;
```

Тип CategoryKey описывает тип ключей, необходимых для рендеринга категории товара

```
type CategoryKey = keyof typeof categoryMap;
```

#### Класс CardCatalogView
Является производным от класса CardFullView. Отвечает за рендеринг карточки товара для каталога

Конструктор:  
- `constructor(container: HTMLElement, actions?: ICardActions)` - принимает ссылку на DOM элемент, за отображение которого он отвечает, для передачи в конструктор родительского класса и обьект actions отвечающий за обработку клика по карточке товара

Поля класса:
Класс не имеет собственных полей (наследует поля от родителя)


Методы класса:
Класс не имеет собственных методов (наследует методы от родителя)


Вспомогательные типы:
Интерфейс ICardActions описывает тип данных параметра actions передаваемого в конструктор класса

```
interface ICardActions {
  onClick(): void;
}
```

#### Класс CardPreviewView
Является производным от класса CardFullView. Отвечает за рендеринг карточки товара для превью

Конструктор:  
- `constructor(container: HTMLElement, actions?: ICardActions)` - принимает ссылку на DOM элемент, за отображение которого он отвечает, для передачи в конструктор родительского класса и обьект actions отвечающий за обработку клика по кнопке добавления/удаления товара

Поля класса:
- `descriptionElement: HTMLElement` - DOM элемент для отображения описания товара
- `buttonElement: HTMLButtonElement` - DOM элемент кнопки добавления/удаления товара


Методы класса:
- `set description(value: string)` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение описания товара
- `set inBasket(value: boolean)` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение состояния кнопки добавления товара (добавить/удалить)

- `set hasPrice(value: boolean)` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение состояния кнопки добавления товара (доступен/недоступен)


Вспомогательные типы:
Интерфейс ICardActions описывает тип данных параметра actions передаваемого в конструктор класса

```
interface ICardActions {
  onClick(): void;
}
```
Тип TCardPreview описывает тип данных, который принимает метод render родительского класса, передается дженериком

```
type TCardPreview = Pick<IProduct, "description">;
```

Тип TButtonState описывает тип данных, который принимает метод render родительского класса, передается дженериком

```
type TButtonState = {
  inBasket: boolean;
  hasPrice: boolean;
}
```

#### Класс CardBasketView
Является производным от класса CardFull. Отвечает за рендеринг карточки товара для корзины

Конструктор:  
- `constructor(container: HTMLElement, actions?: ICardActions)` - принимает ссылку на DOM элемент, за отображение которого он отвечает, для передачи в конструктор родительского класса и обьект actions отвечающий за обработку клика по кнопке удаления товара

Поля класса:
- `indexElement: HTMLElement` - DOM элемент для отображения описания товара
- `buttonElement: HTMLButtonElement` - DOM элемент кнопки удаления товара


Методы класса:
- `set index(value: number)` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение порядкового номера товара



Вспомогательные типы:
Интерфейс ICardActions описывает тип данных параметра actions передаваемого в конструктор класса

```
interface ICardActions {
  onClick(): void;
}
```
Тип TCardBasket описывает тип данных, который принимает метод render родительского класса, передается дженериком

```
type TCardBasket = TCard & {
  index: number;
};
```

#### Класс FormView
Является производным от базового класса Component. Общий абстрактный класс, который наследуют классы, отвечающие за рендеринг форм 

Конструктор:  
- ` constructor(events: IEvents, container: HTMLFormElement)` - принимает экземпляр класса EventEmitter ссылку на DOM элемент, за отображение которого он отвечает, для передачи в конструктор родительского класса

Поля класса:
- `container: HTMLFormElement` - поле для хранения корневого DOM элемента компонента(переопределяет тип соответвующего поля родительского класса)
- `events: IEvents` - экземпляр класса EventEmitter
- `buttonElement: HTMLButtonElement` - DOM элемент кнопки подтверждения перехода к следующему шагу
- `errorElement: HTMLElement` - DOM элемент для отображения ошибок валидации
- `inputElements: HTMLInputElement[]` - DOM элемент содержащий массив элементов полей ввода


Методы класса:
- `set errors(value: string)` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение ошибок валидации
- `set valid(value: boolean)` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение состояния кнопки подтверждения перехода к следующему шагу

- `inputHandler(inputlist: HTMLInputElement[], name: string, value: string)` - метод применяемый в сеттерах дочерних классов для рендеринга значений полей ввода

- `eventEmit(field: string, value: string)` - метод для обработки значений полей ввода, передающий данные для эмита события form:changed


Вспомогательные типы:
Интерфейс IFormErrors описывает тип данных, который принимает метод render родительского класса, передается дженериком

```
interface IFormErrors extends IErrors {
  valid: boolean;
  errors: string;
}
```

#### Класс OrderFormView
Является производным от класса FormView. Отвечает за рендеринг формы обработки данных заказа

Конструктор:  
- `constructor(events: IEvents, container: HTMLFormElement)` - принимает экземпляр класса EventEmitter ссылку на DOM элемент, за отображение которого он отвечает, для передачи в конструктор родительского класса

Поля класса:
- `switchElement: HTMLButtonElement[]` - DOM элемент содержащий массив кнопок выбора типа оплаты


Методы класса:
- `set address(value: string)` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение значения поля ввода адреса достаки
- `set payment(value: string)` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение состояния кнопок выбора типа оплаты

#### Класс ContactsFormView
Является производным от класса FormView. Отвечает за рендеринг формы обработки данных покупателя

Конструктор:  
- `constructor(events: IEvents, container: HTMLFormElement)` - принимает экземпляр класса EventEmitter ссылку на DOM элемент, за отображение которого он отвечает, для передачи в конструктор родительского класса

Поля класса:
Класс не имеет собственных полей (наследует поля от родителя)


Методы класса:
- `set email(value: string)` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение значения поля ввода электронной почты
- `set phone(value: string)` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение значения поля ввода номера телефона

#### Класс SuccessView
Является производным от класса базового класса Component. Отвечает за рендеринг компонента подверждения успешного заказа

Конструктор:  
- `constructor(events: IEvents, container: HTMLFormElement)` - принимает экземпляр класса EventEmitter ссылку на DOM элемент, за отображение которого он отвечает, для передачи в конструктор родительского класса

Поля класса:
- `events: IEvents` - экземпляр класса EventEmitter
- `buttonElement: HTMLButtonElement` - DOM элемент кнопки подверждения
- `priceElement: HTMLElement` - DOM элемент для отображения суммы списаных средств


Методы класса:
- `set price(value:number)` - сеттер, служит для обработки данных, полученных в аргументе медода render родительского класса, отвечает за отображение суммы списаных средств

Вспомогательные типы:
Интерфейс ISuccess описывает тип данных, который принимает метод render родительского класса, передается дженериком

```
interface ISuccess {
  content: HTMLElement;
  price: number
}
```

### Презентер
Презентер, описывающий логику взаимодействия слоя представления и слоя данных реализован в файле `main.ts`. В презентере созданы экземпляры всех классов, используемых приложением, обьявленные необходимые переменные после чего происходит обработка событий генерируемых остальными слоями приложения.



### События 

Все события генерируемые с помощью брокера событий типизированы с помощью enum

```
enum EventType {
  catalogChanged = "catalog:changed",
  selectedCardChanged = "selectedCard:changed",
  cardSelect = "card:select",
  basketOpen = "basket:open",
  basketAdd = "basket:add",
  basketRemove = "basket:remove",
  basketChanged = "basket:changed",
  formChanged = "form:changed",
  buyerChanged = "buyer:changed",
  orderStart = "order:start",
  orderNext = "order:next",
  orderPost = "order:post",
  orderComplete = "order:complete",
}
```

Описание событий:
- `catalog:changed` - событие генерируемое экзепляром класса модели каталога (CatalogModel) в ответ на изменение списка товаров (setProductList())
- `selectedCard:changed` - обытие генерируемое экзепляром класса модели каталога (CatalogModel) в ответ на изменение выбранной карточки (setProduct())
- `card:select` - событие генерируемое экзепляром класса представления карточки каталога (CardCatalogView) в ответ на клик по карточке каталога
- `basket:open` - событие генерируемое экзепляром класса представления хедера (HeaderView) в ответ на клик по кнопке корзиты товаров
- `basket:add` - событие генерируемое экзепляром класса представления карточки превью (CardPreviewView) в ответ на клик по кнопке добавления товара в корзину
- `basket:remove` - событие генерируемое экзеплярами классов представления карточки превью и карточки корзины (CardPreviewView,CardBasketView) в ответ на клик по кнопке удаления товара из корзины 
- `basket:changed` - событие генерируемое экзепляром класса модели корзины (BasketModel) в ответ на добавление, удаление товара из корзины и на очистку корзины (addProduct(), deleteProduct(), clearBasket())
- `form:changed` - событие генерируемое экзеплярами классов представления форм (OrderFormView,ContactsFormView) в ответ на изменение полей ввода и состояний кнопок выбора типа оплаты
- `buyer:changed` - событие генерируемое экзепляром класса модели покупателя (BuyerModel) в ответ на изменение данных покупателя (setData(),clearData())
- `order:start` - событие генерируемое экзепляром класса представления корзины (BasketView) в ответ на клик по кнопке оформления заказа
- `order:next` - событие генерируемое экзепляром класса представления формы данных заказа (OrderFormView) в ответ на клик по кнопке "далее"
- `order:post` - событие генерируемое экзепляром класса представления формы данных поркупателя (ContactsFormView) в ответ на клик по кнопке "оплатить"
- `order:complete` - событие генерируемое экзепляром класса представления подтвеждения успешного заказа (SuccessView) в ответ на клик по кнопке "за новыми покупками"





